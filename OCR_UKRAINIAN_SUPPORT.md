# Підтримка української мови в OCR

## Огляд змін

DocScanLite тепер підтримує розпізнавання українського тексту за допомогою **Tesseract OCR**.

### Що було змінено?

1. **Додано Tesseract OCR Engine**
   - Повна підтримка української мови (кирилиця)
   - Підтримка 100+ мов
   - Високу точність розпізнавання
   - Працює повністю offline

2. **Оновлено OCR модуль**
   - `TesseractTextRecognizer` - новий recognizer з підтримкою української
   - `OcrProcessor` - оновлено для роботи з Tesseract
   - Автоматичний fallback на ML Kit якщо Tesseract недоступний

3. **Залежності**
   - Додано `tesseract4android:4.7.0`
   - Збережено ML Kit як fallback варіант

## Швидкий старт

### 1. Завантаження мовних даних

**Опція А: Автоматичне завантаження (майбутнє оновлення)**
При першому запуску додаток автоматично завантажить українські мовні дані.

**Опція Б: Ручне завантаження**

1. Завантажте `ukr.traineddata` з офіційного репозиторію:
   https://github.com/tesseract-ocr/tessdata_best/raw/main/ukr.traineddata

2. Помістіть файл в:
   ```
   DocScanLite/ocr/src/main/assets/tessdata/ukr.traineddata
   ```

3. (Опціонально) Завантажте `eng.traineddata` для англійської:
   https://github.com/tesseract-ocr/tessdata_best/raw/main/eng.traineddata

### 2. Використання в коді

```kotlin
// Ініціалізація
val ocrProcessor: OcrProcessor = // inject через Hilt
ocrProcessor.initialize("ukr+eng") // Українська + англійська

// Розпізнавання тексту
val bitmap: Bitmap = // ваше зображення
val result = ocrProcessor.processImage(bitmap)

result.onSuccess { ocrResult ->
    println("Розпізнаний текст: ${ocrResult.text}")
    println("Точність: ${ocrResult.confidence}")
    println("Час обробки: ${ocrResult.processingTimeMs} мс")
}
```

### 3. Перевірка стану

```kotlin
// Перевірити чи готовий OCR
if (ocrProcessor.isReady()) {
    // Готовий до роботи
}

// Дізнатися який движок використовується
val engineName = ocrProcessor.getEngineName()
println("Поточний OCR движок: $engineName") // "Tesseract OCR" або "ML Kit (Latin only)"
```

## Архітектура

### Модулі OCR

```
ocr/
├── src/main/java/com/docscanlite/ocr/
│   ├── OcrProcessor.kt                      # Головний процесор (управляє движками)
│   ├── TextRecognizer.kt                    # Інтерфейс recognizer
│   ├── mlkit/
│   │   └── MLKitTextRecognizer.kt          # ML Kit implementation (fallback)
│   ├── tesseract/
│   │   └── TesseractTextRecognizer.kt      # Tesseract implementation (основний)
│   └── model/
│       └── OcrResult.kt                     # Модель результату
└── src/main/assets/
    └── tessdata/
        ├── ukr.traineddata                  # Українська мова
        └── eng.traineddata                  # Англійська мова (опціонально)
```

### Потік роботи

1. **Ініціалізація**
   ```
   OcrProcessor.initialize()
   ↓
   TesseractTextRecognizer.initialize()
   ↓
   Копіювання мовних файлів з assets → filesDir
   ↓
   Ініціалізація Tesseract API
   ```

2. **Розпізнавання**
   ```
   OcrProcessor.processImage(bitmap)
   ↓
   TesseractTextRecognizer.recognizeText(bitmap)
   ↓
   Tesseract API processing
   ↓
   OcrResult (text, blocks, lines, elements, confidence)
   ```

## Порівняння движків

| Характеристика | Tesseract OCR | ML Kit |
|----------------|---------------|---------|
| **Українська мова** | ✅ Повна підтримка | ❌ Не підтримується |
| **Точність** | 🟢 Висока (90-95%) | 🟡 Середня для Latin |
| **Швидкість** | 🟡 Середня (1-3 сек) | 🟢 Швидко (< 1 сек) |
| **Розмір** | 🔴 ~12 MB на мову | 🟢 ~5 MB |
| **Offline** | ✅ Так | ✅ Так |
| **Підтримка мов** | 🟢 100+ мов | 🔴 Обмежено (Latin, Chinese, Japanese, Korean, Devanagari) |

## Конфігурація для різних сценаріїв

### Тільки українська

```kotlin
ocrProcessor.initialize("ukr")
```

### Українська + англійська (рекомендовано)

```kotlin
ocrProcessor.initialize("ukr+eng")
```

### Багато мов

```kotlin
ocrProcessor.initialize("ukr+eng+rus+pol") // Українська, англійська, російська, польська
```

### Fallback на ML Kit

Якщо Tesseract не вдалося ініціалізувати (відсутні мовні файли), автоматично використовується ML Kit:

```kotlin
val result = ocrProcessor.initialize("ukr")
if (result.isFailure) {
    // Автоматично перемкнулося на ML Kit
    println("Using fallback: ${ocrProcessor.getEngineName()}")
}
```

### Ручне перемикання

```kotlin
// Перемкнутися на Tesseract
ocrProcessor.switchToTesseract("ukr+eng")

// Перемкнутися на ML Kit
ocrProcessor.switchToMLKit()
```

## Налаштування якості

### Покращення точності розпізнавання

1. **Використовуйте tessdata_best**
   - Найкраща точність (~10-15 MB на мову)
   - Рекомендовано для production

2. **Попередня обробка зображення**
   ```kotlin
   // Застосуйте фільтри перед OCR
   val processedBitmap = imageProcessor.apply {
       filter = Filter.BLACK_AND_WHITE  // Контрастне зображення
       brightness = 0.2f                 // Підвищена яскравість
       contrast = 0.3f                   // Підвищений контраст
   }

   val result = ocrProcessor.processImage(processedBitmap)
   ```

3. **Роздільна здатність**
   - Мінімум 300 DPI для друкованого тексту
   - 400-600 DPI для дрібного тексту
   - Уникайте занадто великих зображень (> 4000px)

## Performance Tips

### Оптимізація швидкості

1. **Використовуйте tessdata_fast для швидкості**
   - Швидше в 2-3 рази
   - Трохи нижча точність

2. **Downsample великі зображення**
   ```kotlin
   val maxSize = 2048
   val scaled = if (bitmap.width > maxSize || bitmap.height > maxSize) {
       Bitmap.createScaledBitmap(bitmap, newWidth, newHeight, true)
   } else {
       bitmap
   }
   ```

3. **Обробка в background**
   ```kotlin
   viewModelScope.launch(Dispatchers.IO) {
       val result = ocrProcessor.processImage(bitmap)
       withContext(Dispatchers.Main) {
           // Update UI
       }
   }
   ```

### Оптимізація пам'яті

1. **Cleanup після використання**
   ```kotlin
   override fun onDestroy() {
       ocrProcessor.cleanup()
       super.onDestroy()
   }
   ```

2. **Recycle bitmaps**
   ```kotlin
   val result = ocrProcessor.processImage(bitmap)
   bitmap.recycle() // Звільнити пам'ять
   ```

## Troubleshooting

### Проблема: Низька точність

**Рішення:**
- Використовуйте `tessdata_best`
- Покращте контрастність та яскравість зображення
- Використовуйте фільтр BLACK_AND_WHITE або DOCUMENT
- Переконайтесь що текст чіткий (не розмитий)

### Проблема: Повільна обробка

**Рішення:**
- Використовуйте `tessdata_fast`
- Зменшіть роздільну здатність зображення
- Обробляйте в background thread
- Обробляйте тільки необхідну область (crop)

### Проблема: OutOfMemory

**Рішення:**
- Зменшіть розмір зображення перед обробкою
- Викликайте `bitmap.recycle()` після обробки
- Використовуйте `BitmapFactory.Options` з `inSampleSize`

## Відомі обмеження

1. **Перша ініціалізація**
   - Копіювання мовних файлів може зайняти 2-5 секунд
   - Відбувається тільки раз (при першому запуску)

2. **Розмір APK**
   - Додає ~12-15 MB на мову до розміру APK
   - Рекомендується використовувати Android App Bundle для зменшення розміру

3. **Рукописний текст**
   - Tesseract оптимізований для друкованого тексту
   - Рукописний текст розпізнається погано

## Подальший розвиток

### Заплановані покращення

- [ ] Автоматичне завантаження мовних даних з інтернету
- [ ] Кешування результатів OCR
- [ ] Batch processing для кількох зображень
- [ ] Підтримка PDF з OCR
- [ ] Покращена підтримка таблиць
- [ ] Налаштування PSM (Page Segmentation Mode)
- [ ] Whitelist/blacklist символів

## Корисні посилання

- [Tesseract OCR Документація](https://tesseract-ocr.github.io/)
- [Tesseract4Android GitHub](https://github.com/adaptyteam/tesseract4android)
- [Tesseract Language Data](https://github.com/tesseract-ocr/tessdata_best)
- [OCR Best Practices](https://github.com/tesseract-ocr/tesseract/wiki/ImproveQuality)

## Ліцензія

- Tesseract OCR: Apache License 2.0
- Tesseract4Android: Apache License 2.0
- Мовні дані: Apache License 2.0
